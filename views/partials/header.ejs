<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/styles.css" />
</head>

<body>
    <header>

        <a href="/" class="nav-link site-title-link">
            <h1>Message Board</h1>
        </a>
        <% if (currentUser) { %>
            <p>Welcome, <%= currentUser.full_name %>
            </p>
            <% } %>
                <nav>

                    <% if (currentUser) { %>

                        <a class="nav-link border" href="/log-out">Log-out</a>

                        <% if (currentUser && currentUser.is_member) { %>
                            <form action="/leave-club" method="POST">
                                <button type="submit" class="leave-club-button border"
                                    onclick="return confirm('Are you sure you want to leave the club?')">
                                    Leave Club
                                </button>
                            </form>
                            <% } else { %>
                                <a class="nav-link border" href="/become-member">Become a Member</a>
                                <%} %>



                                    <% }else{ %>
                                        <a class="nav-link border" href="/log-in">Log-in</a>

                                        <a class="nav-link" href="/sign-up">Register</a>
                                        <% } %>

                </nav>
    </header>
    <main>